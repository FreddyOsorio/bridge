export declare type LedgerRecordTestStatus = 'verified-hash' | 'verified-signature' | 'invalid-hash' | 'no-signatures' | 'invalid-signature';
export declare class LedgerRecordTestResult {
    status: LedgerRecordTestStatus;
    detail?: string;
    client: string;
    moment: string;
    constructor(status: LedgerRecordTestStatus, detail?: string, moment?: string, client?: string);
}
