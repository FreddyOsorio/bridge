import { LedgerKeyPair, LedgerSigner } from '@minka/types';
import { SignerResponse } from '../clients/signer-client';
import { SignerRecordReadOptions } from '../types/record-builder';
import { CreateRecordBuilder } from './create-record-builder';
export declare class SignerRecordBuilder extends CreateRecordBuilder<LedgerSigner, SignerResponse> {
    private password;
    private decryptSecret;
    private extractKeyPair;
    code(password: string): this;
    lock(): this;
    read(readOptions?: SignerRecordReadOptions): Promise<import("@minka/types").LedgerRecord<LedgerSigner, import("@minka/types").LedgerMeta>>;
    keys(keyPair: Partial<LedgerKeyPair>): this;
}
